# Accelerated Navigation 工程工作规则

产品要求以用户批准的活动设计为准；当前行为以源码和绑定身份的证据为准。详细流程在 `docs/agent-process/`。本文件负责适用范围、路由、授权和质量底线。

## 适用范围与入口

- 普通知识问答、概念澄清、解释已有结论，直接回答，不建立产品批次。
- 不改变产品契约的小范围文档维护，先读相关文件并记录变更和验证即可；工程规则维护需读全待修改规则并检查交叉约束，不迁移产品阶段。
- 产品需求、源码实现、正式审计和正式测试按下列入口执行。任务涉及跨模块状态、并发、持久化、资源或失败策略时使用共同契约的详细记录。

1. 优先读取本任务已知恢复记录。路径未知时读取 `docs/agent-process/current-batch.md` 索引，按任务键、对话授权和目标选择自己的记录；不能按最新时间选择其他任务。没有对应记录时建立 `docs/agent-process/tasks/<task-key>.md` 并追加索引。存在歧义时只暂停有歧义的操作。
2. 首次进入读取本文件、`00-evidence-contract.md` 和对应阶段核心；审计、测试按核心路由追加唯一适用子文件。后续按共同契约核对身份和差异，复用未变内容与有效证据。
3. 用户点名技能，或任务属于技能简介明确触发范围时，读取技能及其必需资源；已读且身份未变时复用。技能不扩大用户授权。
4. 在 commentary 简要说明任务类型或阶段、实际读取或复用的规则、适用技能及影响执行的冲突。无需在每条命令前重复读取恢复栏。

## 阶段与授权

- 需求确认：问题、方案、架构、契约和验收设计，读取 `01-requirements.md`。
- 实现：按批准契约修改生产或测试源码、删旧、编译及必要定向验证，读取 `02-implementation.md`。
- 审计：正式白盒结构、设计一致性、必要性、冗余、兼容及系统复核，读取 `03-audit.md` 和所选 `audit/` 子文件。
- 测试：夹具资格、独立诊断、真实地形、性能、压力和正式验收，读取 `04-testing.md` 和所选 `testing/` 子文件。实现反馈按实现规则执行，不因运行单测自动迁移阶段。

从尚未完成的最早必要阶段开始。四阶段审核门保留；普通“采用”只批准所指决定，“继续”只继续已有授权。用户明确预授权后续阶段、进入条件和范围时，条件成立即可执行并记录转换，不重复请示。接受报告交付不等于接受报告中的失败或未完成对象为通过。

已批准任务范围跨回合、中断和压缩延续，直到用户撤销、改变范围或实际工作越过契约。批准实现包括受影响编译及有意义的定向验证，并允许范围内修复循环；新能力、所有权、线程、格式、资源或失败策略等实质变化仍需决策。已确定契约内的命名、局部数据布局等实现细节无需单独批准。

Git 和子智能体默认仍需本回合明确授权；用户明确指定跨回合预授权及边界时按其原文执行。技能、规则和“继续”不自动授予这些操作，也不授予外部写入或消息发送。

## 记录与恢复

每任务只有一个恢复正文和当前工作批次；共享 `current-batch.md` 只列索引，不保存全工作区唯一当前批次。修改索引前核对并合并现有条目，不能覆盖其他任务。涉及相同文件的并发修改先核对身份和差异，保留可独立合并的修改，冲突只暂停相应文件。

恢复栏记录授权原文、范围、阶段、当前身份、最后检查点、下一动作和未决项。阶段转换、范围或授权变化、完成一组修改及验证、发现阻塞和结束回合时更新；连续执行中复用已确认授权。新回合不自动建立新批次，源码修改也不要求每个补丁独立建档；正式只读核验结束后再修改，核验必须绑定修改后的新身份。

首次完整理解本次所需文件，保留读取范围和身份。截断只补未确认区间；修改后先读差异和关联契约，只有全局约束变化、来源不明或无法确定影响时扩到全文。摘要不能冒充证据，凭证也不能冒充业务正确。

## 质量底线与缺口

- 从真实生产入口确认行为，规范事实及其所有者唯一；派生状态有消费者、依赖身份、失效、寿命和资源边界。
- 生命周期覆盖等待、执行、发布、回调、失败、取消、卸载、关闭和清理；整体就绪不能以无关队列或占位事实代替。
- 新路径接管时删除被替代旧路径。未发布内部接口、持久化数据和旧测试没有默认兼容价值；不为测试反向塑造生产接口。
- 正式审计保持只读，完整处理可核对范围后形成报告；实现反馈允许修改、验证、修复循环。编译、诊断、夹具资格、结构审计和正式验收各自只支持对应结论。
- 缺证按依赖范围分级：基础身份或授权缺失时暂停依赖操作；局部缺口不阻塞独立工作。不虚构覆盖、成功、性能或用户批准。
- 冲突和违规按 `00-evidence-contract.md` 保留最后可信证据及恢复路径。规则修改针对缺失、歧义或冲突；执行未遵守不能靠复制规则掩盖。Markdown 不保证消除语义错误或越权。
